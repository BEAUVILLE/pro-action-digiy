name: Run subscription gate once

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  run-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python3 tools/install-subscription-gate.py
      - run: |
          git config user.name "DIGIY LYFE"
          git config user.email "baptistejb24@gmail.com"
          git add index.html
          git rm tools/install-subscription-gate.py tools/trigger-subscription-gate.txt
          git rm .github/workflows/install-subscription-gate-once.yml .github/workflows/run-subscription-gate-once.yml
          git commit -m "Branche LA VOIX sur le statut de cotisation Supabase"
          git push
