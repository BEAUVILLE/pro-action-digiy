name: Fix ACTION PRO written search 7 languages

on:
  push:
    branches:
      - feature/action-pro-intents-separated-v4-1
    paths:
      - tools/fix_action_pro_intents_v4_1.py
      - .github/workflows/fix-action-pro-intents-v4-1.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/action-pro-intents-separated-v4-1
          fetch-depth: 0

      - name: Apply multilingual written-search fix
        run: python3 tools/fix_action_pro_intents_v4_1.py

      - name: Check JavaScript syntax
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          html=Path('index.html').read_text(encoding='utf-8')
          scripts=re.findall(r'<script(?![^>]*src=)(?![^>]*application/ld\+json)[^>]*>(.*?)</script>',html,flags=re.S|re.I)
          out=Path('/tmp/action-written-js');out.mkdir(exist_ok=True)
          for i,script in enumerate(scripts):
              (out/f'{i}.js').write_text(script,encoding='utf-8')
          print(len(scripts))
          PY
          for file in /tmp/action-written-js/*.js; do node --check "$file"; done
          node --check annuaire-public-digiy.js
          git diff --check

      - name: Verify routing and written search
        run: |
          grep -Eq 'action-pro-7lang-monofichier-20260731-v4-(1-intents|2-written)' index.html
          grep -q 'isRestaurantReservationText(t) && !namedBaptisteHit' index.html
          grep -q 'audio écouter vision' index.html
          grep -q 'assistant aide guide' index.html
          ! grep -q 'audio écouter vision assistant aide guide' index.html
          if grep -q 'v4-2-written' index.html; then
            grep -q 'DIGIY CORE MULTILINGUE ÉCRIT 7L' index.html
            grep -q 'fontaner' index.html
            grep -q 'klempner' index.html
            grep -q 'idraulic' index.html
            grep -q 'loodgieter' index.html
            grep -q 'سباك' index.html
            grep -q 'annuaire-public-digiy.js?v=20260731-routing-v4-2' index.html
          fi

      - name: Commit public files
        run: |
          if git diff --quiet -- index.html annuaire-public-digiy.js; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html annuaire-public-digiy.js
          git commit -m 'Fiabiliser la recherche écrite dans les 7 langues'
          git push origin HEAD:feature/action-pro-intents-separated-v4-1
